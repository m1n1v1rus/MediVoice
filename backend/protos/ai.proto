syntax = "proto3";

package ai;

service AIService {
  // Bidirectional streaming of audio chunks and JSON metadata
  rpc StreamAudio (stream AudioChunk) returns (stream AudioChunk);
}

message AudioChunk {
  bytes data = 1;
  string metadata = 2;  // JSON metadata (e.g., {"type": "audio", "speechEnded": true})
}
