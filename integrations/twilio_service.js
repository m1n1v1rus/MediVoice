// Twilio placeholder layout
export const sendSMS = async (to, body) => {
  console.log(`Sending SMS to ${to}: ${body}`);
};

export const sendWhatsApp = async (to, body) => {
  console.log(`Sending WhatsApp to ${to}: ${body}`);
};
